# Your global npm points at a work CodeArtifact registry whose token has expired,
# which makes `npx wrangler` fail before it starts. This pins just this directory
# back to the public registry so the commands below work.
registry=https://registry.npmjs.org/
